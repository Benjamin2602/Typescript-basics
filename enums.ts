// there are certain times when you want to restrict somebody's choice 

enum seatChoice{
    window = 12,
    aisle,
    fourth = "ben",
    middle = 69,
}

const seat = seatChoice.window;

export {}
